import React from 'react';
import { ScrollView, StyleSheet, Switch, Text, TouchableOpacity, View } from 'react-native';
import { useAutoPlay } from '../hooks/useAutoPlay';
import { useContentCache } from '../hooks/useContentCache';
import { theme } from '../utils/theme';
export function SettingsScreen() { const { isAutoPlayEnabled, setAutoPlay, playlist } = useAutoPlay(); const cache = useContentCache(); return <ScrollView style={styles.container}><Text style={styles.title}>Receiver Settings</Text><View style={styles.row}><Text style={styles.text}>Auto-play received content</Text><Switch value={isAutoPlayEnabled} onValueChange={setAutoPlay} /></View><Text style={styles.text}>Playlist items: {playlist.length}</Text><Text style={styles.text}>Cache: {Math.round(cache.cacheSize / 1024)} KB / {Math.round(cache.availableSpace / 1024 / 1024)} MB</Text><TouchableOpacity style={styles.button} onPress={cache.clear}><Text style={styles.buttonText}>Clear Cache</Text></TouchableOpacity></ScrollView>; }
const styles = StyleSheet.create({ container: { flex: 1, backgroundColor: theme.colors.background, padding: 16 }, title: { color: theme.colors.text, fontSize: 30, fontWeight: '900', marginVertical: 16 }, row: { backgroundColor: theme.colors.surface, borderRadius: 16, padding: 16, marginBottom: 12, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, text: { color: theme.colors.text, marginBottom: 12 }, button: { backgroundColor: theme.colors.primaryDim, padding: 14, borderRadius: 12, alignItems: 'center', marginTop: 12 }, buttonText: { color: theme.colors.text, fontWeight: '800' } });
